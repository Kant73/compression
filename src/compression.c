/*
	Projet CPS - Compression LZW
	Adrien DELAPORTE - Quentin DUNAND - Elsa NAVARRO - Florian POPEK
	compression.c
*/

#include <stdio.h>
#include <string.h>

#include "../include/compression.h"

/**
 * [encode description]
 * @param
 * @param
 * @param
 */
void encode(file* E, file* S, dico* dico)
{
uint8_t a;		//Pour stocker un octet
uint64_t w; 	//Chaine d'octet (dont on va verifier la présence dans le dico)
E = fopen("toto.txt", "r");
if (fichier != NULL) {
	while(!feof(E))
	{
		a = 
	}
}

}
